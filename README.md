# PMP — Fahd Website (static site)

Deployable static build of the **PMP practice-quiz** app — a single self-contained
`index.html` (no server, no build step, no dependencies). This repo holds **only** the
public site so it deploys cleanly on static web hosts (Hostinger, etc.).

- **Live file:** `index.html` — open it in any browser, or serve it at a domain root.
- **Source & build pipeline:** https://github.com/Fajamaan/PMP-Fahd-Website

This file is generated; edit the question banks in the source repo and rebuild rather than
editing `index.html` by hand.
